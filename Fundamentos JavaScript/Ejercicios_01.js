//Ejercicios de Operadores
/**
 * Escribe un programa que calcule el número de segundos que exiten 
 * en un día.
 * 
 */

let dia = 24;// 24 horas = 1 día 
let seg = 3600; // 3600segundos = 1 hora
let numero_segundos_dia;
numero_segundos_dia = dia * seg;
console.log("El número de segundos que exite en un dia es de:"+numero_segundos_dia);

/**
 * 
 * Programa que calcule el área de un rectángulo a partir de los 
 * valores ancho = 2 y alto =5
 */

let ancho = 4,alto =5.2,area;
area = ancho * alto;
console.log("El área de un rectángulo es de:"+area+" que tiene como ancho:"+ancho+" y alto:"+alto);

/**
 * Hacer una conversión de grados a centigrados a grados Fahrenheit
 * 
 */
let gradosFr, gradoscen;
gradoscen = 23;
gradosFr = (gradoscen*9/5)+32;
console.log("Los grados centigrados "+gradoscen+ " a Fahrenheit son "+gradosFr);


